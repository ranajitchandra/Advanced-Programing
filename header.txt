
























<NavLink
                to={to}
                className={({ isActive }) =>
                    `text-lg font-medium leading-7 transition-all duration-300 group ${isActive && isLast ? "text-gray-900" : "text-gray-500 hover:text-gray-900"
                    }`
                }
            >
                {({ isActive }) => (
                    <>
                        <span>{label}</span>
                        <div
                            className={`bg-gray-900 h-0.5 ${isActive && isLast ? "w-full" : "w-0 group-hover:w-full"
                                } transition-all duration-300 mt-0.5`}
                        />
                    </>
                )}
            </NavLink>





